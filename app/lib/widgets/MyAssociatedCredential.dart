import 'package:flutter/material.dart';

class MyAssociatedCredential extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Credenciales Asociadas'),
      ),
      body: Center(
        child: Text('Contenido de Credenciales Asociadas'),
      ),
    );
  }
}